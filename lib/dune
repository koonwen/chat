; (library
;  (name chat)
;  (libraries cohttp-lwt-unix))

(library
 (name lib)
 (libraries lwt lwt.unix unix logs logs.lwt core core_unix)
 (modules client server middleware))

(env
 (dev
  (flags
   (:standard -w -32-34))))
